#include<stdio.h>
int m;
int prime(int n)
{
if(m==1)
return 1;
else if(n%m==0)
return 0;
else
{
	m=m-1;
	prime(n);
}

	
}
main()
{
	int c,check;
	printf("enter number:");
	scanf("%d",&c);
	
		m=c/2;
	check=prime(c);
	if(check==1)
printf("prime number");
else
printf("not a prime number");

}
